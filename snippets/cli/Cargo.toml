[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.1.15", default-features = false, features = [
    "std",
    "color",
    "derive",
    "unicode",
    "wrap_help",
] }
