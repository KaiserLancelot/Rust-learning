[package]
name = "update"
version = "0.1.0"
edition = "2021"

[dependencies]
self_update = { version = "0.29.0", default-features = false, features = [
    "rustls",
    "archive-zip",
    "archive-tar",
    "compression-flate2",
] }
