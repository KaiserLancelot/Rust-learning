[package]
name = "translation"
version = "0.1.0"
edition = "2021"

[dependencies]
# TODO fork or PR, make it better
simplecc = { version = "0.2.2", default-features = false, features = [
    "builtin_dicts",
] }
mimalloc = { version = "0.1.29", default-features = false }
